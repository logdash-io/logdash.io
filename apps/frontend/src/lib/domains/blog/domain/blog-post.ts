export type BlogPostListItem = {
  id: string;
  title: string;
  createdAt: string;
};

export type BlogPost = {
  id: string;
  title: string;
  body: string;
  createdAt: string;
  updatedAt: string;
};
